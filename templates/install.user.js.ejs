// ==UserScript==
// @name         Fecli Injection Script
// @author       FunnelEnvy
// @homepage     http://www.funnelenvy.com/
// @source       https://github.com/funnelenvy/optimizely-cli
// @namespace    fecli
// @description  Inject local JS / CSS into a Chrome page for development
// @include      /fecli=activate/
// @run-at       document-start
// ==/UserScript==

// Add inject.js
(function addScript () {
  if (!document.head) return setTimeout(addScript, 50);
  var scriptElement = document.createElement('script');
  scriptElement.type = 'text/javascript';
  scriptElement.src =  '//localhost:<%- _port %>/inject.js';
  document.head.appendChild(scriptElement);
})();
